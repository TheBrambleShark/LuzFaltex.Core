# LuzFaltex.Core.Collections

This library adds custom generic collection types that compliment the existing .NET collection types.

- `MultiValueDictionary<TKey, TValue>`, which behaves under the hood like a `Dictionary<TKey, ICollection<TValue>>`.